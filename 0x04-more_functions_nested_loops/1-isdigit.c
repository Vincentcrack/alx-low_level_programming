#include "main.h"

/**
 * _isdigit - checks if a char is digit
 * @c: the int to check
 * Return: 0 or 1
 */
int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
